#include <stdio.h>
#include "lista.h"

void solve() {

}

int main() {
    int r, k, l;
    FILE *in, *out;

    in = fopen("txt.in", "r");
    fscanf(in, "%d %d %d", &r, &k, &l);

    for (int i = 0; i < r; i++) {
        
        fscanf("%s %s %d")
    }


    return 0;
}